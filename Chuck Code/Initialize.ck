//me.dir() + "/MouseInstrument.ck" => string mousePath;
//Machine.add(mousePath) => int mouseID;

//me.dir() + "/artGenerator.ck" => string genPath;
//Machine.add(genPath) => int genID;

2::week => now;

//Machine.remove(mouseID);
Machine.remove(genID);